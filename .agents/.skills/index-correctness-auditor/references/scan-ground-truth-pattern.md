# Scan Ground Truth Pattern

Every index test should have a scan equivalent.

The scan equivalent is the source of truth.

Indexed result must match:
- same rows
- same order when order is defined
- same aggregation result
- same count
